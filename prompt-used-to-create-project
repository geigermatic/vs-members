# Supabase Data Viewer Project Setup

## Overview

This project is designed to create a lightweight viewer for member and financial data from a Supabase database. The setup includes all necessary configurations, dependencies, and instructions to ensure the project "just works."

## Project Structure

- **.cursor-next/templates/supabase-viewer.md**: Contains detailed setup instructions and context for the project.
- **.cursor-next/rules.txt**: Defines the rules and best practices for the project.
- **README.md**: Provides an overview, setup instructions, architecture details, and deployment guidelines.

## Setup Instructions

1. **Initialize the Project:**

   - Navigate to your project directory:
     ```bash
     cd /Users/jg/vs-projects/vs-api
     ```

   - Initialize a new Node.js project:
     ```bash
     npm init -y
     ```

2. **Install Dependencies:**

   - Install the necessary packages:
     ```bash
     npm install @supabase/supabase-js react react-dom typescript prettier
     npm install --save-dev @types/react @types/react-dom @vitejs/plugin-react vite @types/node dotenv
     ```

3. **Set Up Directory Structure:**

   - Create the required directories and copy template files:
     ```bash
     mkdir -p .cursor-next/templates
     cp ../jg-lenders-main/.cursor-next/templates/supabase-viewer.md .cursor-next/templates/
     cp ../jg-lenders-main/.cursor-next/rules.txt .cursor-next/
     ```

4. **Create Essential Files:**

   - Create a `.gitignore` file:
     ```bash
     echo ".env" > .gitignore
     echo "node_modules" >> .gitignore
     echo "dist" >> .gitignore
     ```

   - Create an `.env.example` file:
     ```bash
     echo "VITE_SUPABASE_URL=https://ehdqmnkyiaoqdyifaqma.supabase.co" > .env.example
     echo "VITE_SUPABASE_KEY=your_key_here" >> .env.example
     ```

5. **Initialize Git:**

   - Set up Git and make the initial commit:
     ```bash
     git init
     git add .cursor-next/ .gitignore
     git commit -m "Initial commit: Project rules and templates"
     ```

6. **Push to GitHub:**

   - Add the remote and push to GitHub:
     ```bash
     git remote add origin https://github.com/jgonis/vs-api.git
     git branch -M main
     git push -u origin main
     ```

7. **Run the Development Server:**

   - Start the development server:
     ```bash
     npm run dev
     ```

## Deployment

- **Local Build Test:**
  ```bash
  npm run build
  npm run preview
  ```

- **Netlify Deployment:**
  1. Install Netlify CLI:
     ```bash
     npm install -g netlify-cli
     ```

  2. Create `netlify.toml`:
     ```toml
     [build]
       publish = "dist"
       command = "npm run build"

     [[redirects]]
       from = "/*"
       to = "/index.html"
       status = 200
     ```

  3. Set up environment variables in Netlify:
     - VITE_SUPABASE_URL: https://ehdqmnkyiaoqdyifaqma.supabase.co
     - VITE_SUPABASE_KEY: [your-key]

  4. Deploy:
     ```bash
     netlify build
     netlify deploy
     netlify deploy --prod
     ```

## Additional Notes

- Ensure the `.env` file is updated with your actual Supabase credentials.
- Follow the architecture and development guidelines in the README.md for best practices.


////////
Prompt used to create project:

# Project Generation Prompt for Supabase Data Viewer

Please help me create a React + TypeScript application for viewing member and financial data from Supabase. Here are the specifications:

## Project Overview
- Single page application for viewing member data and financial statistics
- Uses Supabase for data storage and retrieval
- Built with React 18, TypeScript, and Vite
- Includes real-time API metrics display

## Core Requirements
1. Connect to Supabase database
2. Display member details
3. Show financial statistics
4. Include loading states
5. Handle errors appropriately
6. Use TypeScript strict mode
7. Use real Supabase connection (no mock data)
8. Use proper React 18 TypeScript patterns:
   - Use FC type for components
   - Import from 'react-dom/client'
   - Use createRoot instead of render
   - Remove unnecessary React imports

## Environment Variables
```env
VITE_SUPABASE_URL=https://ehdqmnkyiaoqdyifaqma.supabase.co
VITE_SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVoZHFtbmt5aWFvcWR5aWZhcW1hIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDAwOTk4MDMsImV4cCI6MjA1NTY3NTgwM30.k6N0PQqrvbQ0mJnjDynHaYPTXIfSdBxxeBCLSQGurbw
```

## Type Definitions
Need interfaces for:
1. Member data
2. Financial health statistics
3. API metrics
4. Request/response tracking
5. React component types (FC)

## TypeScript Configuration
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "types": ["vite/client"],
    "allowJs": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

## Component Template
```typescript
import { FC, useEffect, useState } from 'react';

const ComponentName: FC = () => {
  // Component logic here
  return (
    <div>
      {/* Component JSX */}
    </div>
  );
};

export default ComponentName;
```

## Required Files Structure
```
/src
  /api
    supabaseClient.ts
    memberApi.ts
  /components
    MemberView.tsx
    FinancialStats.tsx
  /types
    index.ts
    member.ts
    financialStats.ts
  /utils
    formatters.ts
  App.tsx
  main.tsx
```

## Package.json Scripts
```json
{
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1",
    "netlify:dev": "netlify dev",
    "netlify:build": "netlify build",
    "netlify:deploy": "netlify deploy --prod"
  }
}
```

## Data Models
1. Member:
   - UUID
   - Name (first, last)
   - Email
   - Demographics
   - Financial info

2. Financial Health Stats:
   - Monthly records
   - Various financial metrics
   - Verascore calculations

## Import Patterns
1. React Components:
   ```typescript
   import { FC, useEffect, useState } from 'react';
   ```
2. Types:
   ```typescript
   import type { Member } from '../types/member';
   import type { FinancialHealthStats } from '../types/financialStats';
   ```
3. API:
   ```typescript
   import { supabase } from './supabaseClient';
   ```

## API Requirements
1. Fetch member data by UUID
2. Get financial statistics
3. Track API metrics:
   - Response times
   - Data sizes
   - Status codes

## UI Requirements
1. Clean, professional design
2. Loading states
3. Error handling
4. API metrics display
5. Responsive layout

## Testing Data
Use member UUID: "dc922ded-d0a7-415a-9d4c-f1e8605fce92"

Please generate all necessary files and code to create this application. Include proper error handling, TypeScript types, and API integration.